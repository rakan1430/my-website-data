# خُطى (Khuta) — دليل التسليم الشامل للمطوّر الجديد

> **الغرض من هذا الملف**: سجل تقني ووظيفي كامل لكل ما بُني، أُصلح، ووُثِّق في مشروع خُطى منذ بدايته حتى الآن — بما فيه القرارات المتخذة وأسبابها، الأخطاء الحقيقية التي اكتُشفت وكيف أُصلحت، والبنية الحالية الدقيقة لكل نظام فرعي. مكتوب ليكون مرجعاً كافياً بذاته لمطوّر جديد لم يرَ المشروع من قبل إطلاقاً.
>
> **تاريخ التحرير**: 12 أغسطس 2026
> **مصادر هذا الملف**: تجميع من (أ) ملخصات جلسات تطوير سابقة محفوظة في معرفة المشروع، (ب) محادثة تطوير طويلة جداً حديثة غطّت إعادة بناء أنظمة كاملة (الأمان، البريد الإلكتروني، الذكاء الاصطناعي)، (ج) فحص مباشر للكود الفعلي والقاعدة الحية.

---

## 0. خلاصة تنفيذية سريعة (اقرأها أولاً)

خُطى تطبيق ويب (لا تطبيق جوال) لمساعدة الطلاب السعوديين على تنظيم مذاكرتهم لاختبار القدرات العامة (GAT/Qudrat). **الفلسفة الجوهرية التي يجب فهمها قبل أي شيء آخر**: التطبيق يعمل بالكامل كـ"ضيف" بلا حساب إطلاقاً (كل البيانات في `localStorage`)، والحساب اختياري فقط لمزامنة التقدّم بين الأجهزة. هذا القرار يتكرر أثره في كل نظام فرعي تقريباً.

**التقنيات**: HTML/CSS/JS خام بلا أي إطار عمل (React/Vue/إلخ) — ملف واحد ضخم لكل من `index.html` و`app.js`. لا خطوة بناء (build step) — الملفات تُنشر كما هي حرفياً.

**الاستضافة**: Netlify، من مستودع GitHub (`rakan1430/khuta-website`)، يرفعها المستخدم يدوياً (لا CI/CD تلقائي من طرف المطوّر الذكي).

**قاعدة البيانات**: Supabase (Postgres + Auth)، الخطة المجانية.

**خدمات خارجية**: Google Gemini (مساعد ذكي)، Brevo (بريد إلكتروني للطلاب)، Formspree (تقارير المطوّر فقط، اتجاه مختلف تماماً)، Web Push (تذكيرات المتصفح).

---

## 1. نظرة عامة على المشروع والفلسفة المعمارية

### 1.1 ما هو خُطى بالضبط؟
تطبيق ويب ثنائي اللغة (عربي/إنجليزي، لكن العربية هي اللغة الأساسية والواجهة RTL افتراضياً) يجمع بين:
- جدول مذاكرة يومي يُبنى تلقائياً من مصادر الطالب المختارة (كتب/دورات) وموزَّع على أيام خطته
- حاسبة النسبة الموزونة (القبول الجامعي) لأكثر من 30 جامعة سعودية
- حاسبة المعدل التراكمي لنظام "المسارات" الثانوي السعودي
- نظام مؤقّت/جلسة تركيز مع منطق استراحات تلقائي
- محاكي اختبار قدرات كامل (5 أقسام، 24 دقيقة/قسم، يحاكي واجهة "قياس" الحقيقية)
- مساعد ذكاء اصطناعي (Gemini) — دردشة، سبورة تفاعلية للشرح، دفتر رسم يُحلَّل بالذكاء الاصطناعي
- ميزات مجتمع (لوحة صدارة، غرفة مذاكرة حيّة، حائط أسئلة، قوالب خطط مشتركة)
- دليل تخصصات جامعية
- نظام تحفيزي (XP، مستويات، أوسمة، سلسلة أيام متتالية)
- نظام بريد إلكتروني (نتائج الاختبارات، رسائل تذكيرية اختيارية)
- لوحة إدارة داخلية للمطوّر (رسائل البريد، بيانات تجريبية)

### 1.2 القرار المعماري الأهم: "الضيف أولاً" (Guest-First)
كل شيء في التطبيق يعمل بنسبة 100% بلا أي حساب. الحساب (اسم مستخدم/كلمة مرور أو Google) **يضيف فقط** إمكانية مزامنة التقدّم بين أجهزة متعددة — لا يفتح أي ميزة كانت مغلقة قبله. هذا القرار مقصود تماماً لإزالة أي احتكاك عند أول استخدام.

### 1.3 حيلة اسم المستخدم/البريد الوهمي (مهم جداً لفهم نظام الحسابات)
Supabase Auth مبني حول البريد الإلكتروني وكلمة المرور، لكن المنتج يريد اسم مستخدم بسيطاً بلا بريد إلزامي. الحل: كل اسم مستخدم يُحوَّل داخلياً لبريد وهمي بالصيغة:
```
khuta.<اسم_المستخدم>@gmail.com
```
`gmail.com` تحديداً (لا نطاق مخترَع مثل `khuta.local`) لأن Supabase يتحقق من وجود سجلات DNS/MX حقيقية للنطاق — أي نطاق غير مسجَّل فعلياً يُرفض فوراً بخطأ "email address is invalid". هذا البريد **لا يصله شيء أبداً** ولا يراه الطالب، وهو فقط الشكل الذي يتطلبه Supabase داخلياً.

**لهذا بالتحديد**، كل فحص "هل هذا الطالب لديه بريد حقيقي مرتبط؟" في كل نظام لاحق (استرجاع كلمة المرور، بريد نتيجة الاختبار، الرسائل التذكيرية) يعمل بمقارنة البريد الفعلي في `auth.users` بهذا النمط الوهمي المُعاد بناؤه — إن تطابقا، لا يوجد بريد حقيقي؛ إن اختلفا، الطالب ربط بريده الحقيقي صراحة عبر `linkRecoveryEmail()`.

---

## 2. التقنيات والبنية

### 2.1 الخطوط والهوية البصرية
- **العرض/العناوين**: El Messiri
- **النص الأساسي**: IBM Plex Sans Arabic
- **أحادي المسافة** (أرقام، أكواد): IBM Plex Mono
- تم الابتعاد عمداً عن خط "Cairo" (الافتراضي الشائع في كل موقع عربي تقريباً) لإعطاء هوية مميزة.
- **نظام الألوان**: ذهبي (`--gold: #C9962E`) للإنجاز، تركوازي/أخضر للتقدّم والنجاح، وردي/أحمر للتنبيهات. المظهر البصري يُوصف داخلياً بـ"Cosmic Glass" — بطاقات زجاجية شبه شفافة (`backdrop-filter: blur`) فوق خلفية كونية داكنة متدرّجة.
- **نظام خط موحّد رسمي** (أُضيف حديثاً): متغيّرات CSS `--fs-title` (26px) / `--fs-title-sm` (19px) / `--fs-subtitle` (13.5px) / `--fs-body` (14.5px) / `--fs-caption` (11.5px)، مربوطة بـ`h1`/`h2`/`h3` و`.card-sub`/`.hint` تحديداً (الفئتان الأكثر استخداماً في كامل الموقع للعنوان الفرعي والنص الصغير).

### 2.2 بنية الملفات (الوضع الحالي)
```
khuta-website/                          (جذر مستودع GitHub)
├── index.html                          كل الـHTML، الآن بلا CSS داخلي (مُستخرَج بالكامل)
├── app.js                              كل منطق التطبيق (>600 كيلوبايت، ملف واحد)
├── styles.css                          كل الـCSS (مُستخرَج من index.html سابقاً لتحسين الأداء)
├── netlify.toml                        إعداد Netlify: رؤوس أمان، تخزين مؤقت، وظيفة مجدولة
├── .gitignore                          يستثني node_modules/.env (أُضيف حديثاً)
├── robots.txt, sitemap.xml             SEO أساسي
├── manifest.json, sw.js                إعداد PWA أساسي (أيقونة/اسم، عامل خدمة بسيط) — غير مطوَّر بعمق بعد
├── content.json, universities.json,
│   specialties.json, exam-questions.json   ملفات بيانات بديلة/احتياطية (النمط "remote-config"، انظر §2.4)
├── icon-192.png, icon-512.png,
│   icon-512-maskable.png               أيقونات PWA
└── netlify/functions/                  (وظائف خادم Serverless — Node.js)
    ├── gemini-proxy.js                 وسيط آمن لكل استدعاءات الذكاء الاصطناعي
    ├── send-email.js                   وسيط آمن لكل إرسال بريد (Brevo)
    ├── send-reminders.js               وظيفة مجدولة يومية لتذكيرات Push (لم تُمس هذه الجلسة)
    └── package.json                    تبعيات npm لمجلد الوظائف (حالياً: web-push فقط عملياً)
```

**ملاحظة حاسمة عن الوظائف الخادمية (Netlify Functions)**: كل ملف `.js` داخل `netlify/functions/` يتحوّل تلقائياً لوظيفة خادم مستقلة يمكن استدعاؤها عبر `/.netlify/functions/<اسم_الملف_بلا_امتداد>`. **الاعتمادية الخارجية (npm) داخل هذه الوظائف كانت مصدر خلل حقيقي متكرر** (انظر §6) — الحل المعتمَد الآن: **لا اعتماديات خارجية إطلاقاً** في `gemini-proxy.js` و`send-email.js`، فقط `fetch` المدمجة في Node — يتجنّب تماماً مشاكل تحزيم Netlify التي ظهرت مع `@supabase/supabase-js`.

### 2.3 لا خطوة بناء (No Build Step)
الملفات تُنشر كما هي حرفياً — لا Webpack، لا Vite، لا TypeScript يُترجَم. هذا قرار متعمَّد لإبقاء دورة النشر بسيطة لمطوّر غير تقني في الأصل (رفع ملفات مباشرة لـGitHub). **الأثر العملي**: أي تعديل CSS/JS يجب أن يكون صالحاً مباشرة كما هو — لا حِيَل تتطلب مترجماً.

### 2.4 نمط "الإعداد عن بُعد" (Remote-Config) — كيف يُحدِّث مطوّر غير تقني البيانات
مشكلة متكررة بكل نظام بيانات حساس (أوزان الجامعات، محتوى الدورات، منهج المعدل، دليل التخصصات): **المطوّر يحتاج تحديثها دون لمس الكود**. الحل المعتمَد باستمرار:
| البيانات | الطريقة الأساسية | الاحتياطي |
|---|---|---|
| الجامعات (الأوزان، STEP) | جدول Supabase `universities` عبر Table Editor (واجهة شبيهة بجدول بيانات) | `REMOTE_UNIVERSITIES_URL` (JSON على GitHub) أو مصفوفة مدمجة بالكود |
| محتوى الدورات (عدد الأقسام، الوقت لكل وحدة) | `REMOTE_CONTENT_URL` يُعدِّل `CONTENT_CONFIG` | كائن `CONTENT_CONFIG` المدمج |
| منهج المعدل التراكمي | `REMOTE_CURRICULUM_URL` | `SAUDI_CURRICULUM_DEFAULT` المدمج |
| دليل التخصصات | `REMOTE_SPECIALTIES_URL` | مصفوفة `SPECIALTIES` المدمجة |
| بنك أسئلة الاختبار المحاكي | `REMOTE_EXAM_QUESTIONS_URL` | `EXAM_QUESTIONS_LOCAL` المدمجة (أمثلة توضيحية فقط حالياً) |
| **رسائل البريد التذكيرية** (جديد) | جدول Supabase `marketing_messages` عبر Table Editor مباشرة | — |

كل حقل في `CONTENT_CONFIG` و`UNIVERSITIES` موثَّق بتعليقات عربية داخل الكود تشرح بالضبط ماذا يتحكم به كل حقل.


---

## 3. جرد الميزات الكامل (كل نظام فرعي بالتفصيل)

### 3.1 الدخول والحسابات
- شاشة الدخول: **متابعة بحساب Google**، اسم مستخدم/كلمة مرور (دخول أو إنشاء حساب)، أو **متابعة كضيف**.
- تسجيل الدخول بـApple كان مبنياً في مرحلة مبكرة ثم **أُزيل صراحة** بطلب المستخدم (تكلفة/تعقيد حساب مطوّر Apple).
- **قفل تصاعدي عند فشل الدخول المتكرر**: 3 محاولات فاشلة → قفل دقيقة، الرابعة → 5 دقائق، الخامسة فأكثر → 30 دقيقة؛ يُصفَّر تماماً بعد 24 ساعة من عدم النشاط (محلي، فوق حماية Supabase الخادمية).
- عند الدخول الناجح، النسخة السحابية (`user_data.data`) تكتب فوق الحالة المحلية.
- **البريد الإلكتروني اختياري بالكامل** عند إنشاء الحساب — حقل منفصل، يفعّل استرجاع كلمة المرور فوراً إن مُلئ.
- **نظام الموافقة على الرسائل التذكيرية** (جديد): خانة اختيارية عند التسجيل — **مفعّلة افتراضياً للحسابات الجديدة** (يمكن للطالب إلغاءها)، بينما **الحسابات الموجودة مسبقاً** تُسأل مرة واحدة فقط عبر نافذة منبثقة تظهر تلقائياً (فقط لمن لديه بريد حقيقي مرتبط أصلاً) ولا تتكرر أبداً بعد الإجابة.
- **`username_lookup`**: جدول يربط اسم المستخدم ببريده الحالي (يُحدَّث تلقائياً عبر Trigger في قاعدة البيانات) — يحل مشكلة استرجاع كلمة المرور بعد تغيير البريد (انظر §6).

### 3.2 المعالج التدريجي (بناء الخطة) — 10 خطوات
`welcome` → `plans` → `verbal` → `found` (تأسيس كمي) → `train` (تدريب كمي) → `extra` → `pace` (إيقاع الخطة) → `routine` (روتين أسبوعي) → `breaks` → `confirm`

- **اللفظي**: دورة إيهاب فقط (215 قسم، ~7 دقائق/قسم) + مصدر إضافي اختياري.
- **تأسيس كمي**: كتاب المعاصر 10 (تحدي 30 يوم، 8 صفحات/يوم) أو أينشتاين (57 فيديو ~ساعة لكل واحد، مع خيار "مراجعة فقط" 9 فيديوهات، وتحذير صريح أنها دورة قديمة نسبياً) أو تخطّي.
- **تدريب كمي**: اختيار متعدد من المنصف (120 بنك، ~50 دقيقة/بنك)، المفكر أقسام (90 قسم)، المفكر الأكثر تكراراً، بنوك المعاصر، + كتيّب مراجعة ليلة الاختبار اختياري.
- **مصادر مخصصة**: الطالب يقدر يضيف مصدراً لفظياً وكمياً بأسمائه الخاصة — إن سمّاه، يُرسَل تقرير صامت للمطوّر (Supabase + Formspree، انظر §3.14) لرصد المصادر الشائعة المرشَّحة للإضافة الرسمية.
- **إيقاع الخطة**: قوالب جاهزة (مكثّف 30 يوم / متوازن 45 / مريح 60 / مخصّص) **أو** إدخال تاريخ الاختبار مباشرة (يُحسَب عدد الأيام تلقائياً). الوقت اليومي بالساعة+الدقيقة.
- **معاينة إيقاع الخطة** (ميزة جديدة، §6): زر "إظهار التفاصيل" اختياري يعرض حسابياً حقيقياً (لا تقريبياً) — كم يوماً سيحتاجه الطالب لينهي كل مصادره بالوقت اليومي المختار، وكم يوم احتياطي متبقٍ، بإعادة استخدام نفس بيانات `content.json` التي يعتمدها محرّك الجدولة نفسه.
- **الروتين الأسبوعي**: تحديد إيقاع كل يوم (عادي/أخف/إجازة) + استثناء تواريخ محددة (سفر مثلاً).
- خلل CSS تاريخي (`align-items:center` يسبب "قفز" بصري عند تغيّر ارتفاع المحتوى) أُصلح بالتحوّل لتخطيط مثبَّت من الأعلى.

### 3.3 لوحة التحكم ومحرّك الجدولة
- كميات يومية تُحسَب تلقائياً (`Math.ceil(الإجمالي / الأيام)`) من `CONTENT_CONFIG` مركزياً.
- **مسار التقدّم**: سلسلة أفقية من نقاط الأيام (منجز/اليوم/قادم) + حلقة ملخّصة + بطاقات إحصائية.
- **حالة المهمة تلقائية بالكامل** — مرتبطة بمؤقّت الجلسة الفعلي، لا قائمة منسدلة يدوية (أُزيلت عمداً): `inprogress` أثناء الجلسة، `done` فقط إن اكتملت الجلسة الملتزَم بها دون إيقاف مفرط، وإلا تبقى غير مكتملة.
- **قسم "نظرة عامة"** (أُضيف لاحقاً): بطاقة XP سداسية كبيرة، 3 بطاقات إحصائية (السلسلة، ساعات المذاكرة، % الإنجاز)، خريطة نشاط حرارية (أسلوب GitHub، 16 أسبوعاً)، لمحة "مهام اليوم" (تعكس صفوف الجدول الحقيقي مباشرة)، لوحة صدارة مصغّرة.
- **تخصيص اللوحة**: تفعيل/إخفاء البطاقات الاختيارية (الخريطة الحرارية، الصدارة، مسار التقدّم، إلخ) — **بطاقات "نظرة عامة" الأساسية موسومة `reorderable:false` تحديداً** بعد اكتشاف أن "إعادة الضبط الافتراضي" كانت تنقلها بالخطأ لكومة إعادة الترتيب.
- إعادة ترتيب البطاقات عبر أسهم ↑↓ بسيطة (تبسيط متعمَّد من طلب سحب-وإفلات كامل، لتقليل مخاطر التنفيذ).

### 3.4 وضع التركيز (مؤقّت الجلسة) — نظام فرعي كبير
آلة حالة كاملة (`startMainSession`, `mainTick`, `startAutoBreak`, `breakTick`, `togglePauseSession`, `completeMainSession`, `failMainSession`):
- **استراحة تلقائية**: كل 3600 ثانية من **وقت مذاكرة فعلي نشط** (لا وقت جدار الساعة — الإيقاف لا يُحتسَب)، تبدأ استراحة تلقائياً بالمدة المُعدَّة (1-25 دقيقة)، ثم تستأنف الجلسة تلقائياً.
- **استراحات قصيرة**: عدد محدود (0-3، يُعدّه الطالب) من استراحات 5 دقائق يدوية، تُعطَّل بعد الاستنفاد أو أثناء تشغيل الجلسة فعلياً.
- **زر الإيقاف** مخصَّص للطوارئ الحقيقية فقط: توقّف > 5 دقائق → تحذير مرئي؛ توقّف ≥ 10 دقائق (تراكمياً) → **فشل الجلسة**: مهام اليوم تعود غير مكتملة، لا XP، لا احتساب سلسلة.
- طلب ملء الشاشة عند بدء الجلسة (أفضل جهد فقط عبر Fullscreen API — ليس قفلاً حقيقياً).
- **الهوية الزجاجية الكاملة**: مؤقّت زجاجي مع انعكاس ضوئي يتتبّع الفأرة، ظل داخلي، حلقة تقدّم — نفس التنفس 2%/4 ثوانٍ، نجوم منجرفة ببطء (~0.1px/ثانية).
- **32 نصيحة تحفيزية** تتغيّر عشوائياً **مع كل جلسة جديدة فقط** (لا مع كل فتح/إغلاق).
- **خلفيات وضع التركيز**: تجربة تصميم طويلة انتهت بـ5 ثيمات SVG مرسومة يدوياً (ليل نجمي، غروب، مكتب وقهوة، محيط، فجر) — مرّت بمحاولة صور مولَّدة بالذكاء الاصطناعي (Gemini) استُبعِدت لاحقاً بسبب جودة منخفضة عند التمديد على شاشات عريضة (دقة 1024px فقط).
- **نسخة الهاتف**: شريط علوي يحترم `env(safe-area-inset-top)` (حل تداخل مع الساعة/البطارية)، بطاقتا المهمة/XP كشريط أفقي قابل للتمرير.
- **زر تبديل الوضع الداكن/الفاتح داخل وضع التركيز على الهاتف تحديداً** (خلل أُصلح حديثاً — كان موجوداً فقط في رأس عائم حصري للكمبيوتر، فلم يكن متاحاً للهاتف إطلاقاً؛ أُضيف زر مكافئ في الشريط العلوي الدائم الظهور، بشرط إظهاره على الهاتف فقط لتفادي التكرار مع نسخة الكمبيوتر).

### 3.5 محاكي الاختبار (Exam Simulator)
واجهة تحاكي منصة "قياس" الحقيقية بدقة:
- **البنية**: 120 سؤال، 5 أقسام، 24 دقيقة/قسم بالضبط، توزيع 65/55 كمي/لفظي عشوائي (أو العكس)، 11 كمي + 13 لفظي لكل قسم تقريباً.
- **تقدّم للأمام فقط** بين الأقسام (مقفولة بعد الإنهاء، مطابقةً للاختبار الحقيقي).
- شريط جانبي: مؤقّت القسم، بطاقة المستخدم، إحصاءات (باقٍ من القسم/تمت الإجابة/تمت الزيارة/لم تُزَر)، لوحة تنقّل أسئلة **مقيَّدة بالقسم الحالي**، أزرار (تعليمات القسم/الاختبار، المعادلات، إنهاء القسم، خروج).
- تحكم بحجم الخط (A-/A/A+)، تمييز سؤال للمراجعة، حفظ والتالي.
- **وضع المراجعة بعد التسليم**: يفتح كل الأقسام، يلوّن الصحيح/الخاطئ.
- `FEATURE_EXAM_SIMULATOR` **مفعَّل الآن (`true`)** — كان `false` عمداً لفترة طويلة (البنك كان أمثلة توضيحية فقط)، فُعِّل بعد نضج الميزة.
- **يتكيّف تلقائياً** مع حجم بنك الأسئلة الفعلي دون تكرار سؤال، ويحذّر المستخدم صراحة إن كان الاختبار "مصغَّراً" بسبب قلة الأسئلة المتاحة.
- **معاملة خاصة للهاتف** (جديد): تنبيه اختياري (مرة واحدة/جلسة متصفح) يوضح أن التجربة أفضل على الكمبيوتر، مع خيار المتابعة على الهاتف بعرض مُهيَّأ أوسع خصيصاً لذلك.
- **مولِّد اختبار من ملفات الطالب الخاصة**: يرفع الطالب ملف محتوى دراسي، والذكاء الاصطناعي يولِّد أسئلة اختيار من متعدد منه مباشرة (نمط JSON صارم، انظر §3.9).

### 3.6 الحاسبات
**النسبة الموزونة**: 30+ جامعة سعودية (أوزان الثانوية/القدرات/التحصيلي/STEP الاختياري)، مع **منطق STEP بثلاث حالات**:
- `true` (إجباري) — ينقسم لـ"موزون" (له % رقمي بالمعادلة) أو "بوابة فقط" (شرط اجتياز لا يدخل الحساب الرقمي، مثل جامعة الملك فهد).
- `"partial"` — إجباري لكليات محددة فقط (كالطب).
- `false` — لا دليل على اشتراطه.
- بيانات الجامعات مصدرها **جدول Supabase قابل للتعديل عبر Table Editor** — اختير هذا تحديداً لأنه أسهل لمطوّر غير تقني من تعديل JSON على GitHub.

**المعدل التراكمي**: معدل السنة = مجموع(الدرجة×ساعات المادة) ÷ مجموع الساعات (وزن بالساعات، لا متوسط بسيط). المعدل النهائي = (سنة1×20%) + (سنة2×40%) + (سنة3×40%) — الأوزان اللاحقة أكبر عمداً بتصميم نظام "المسارات" نفسه، يعطي الطالب فرصة تعويض سنة أولى ضعيفة.

### 3.7 دليل التخصصات
~38 تخصصاً (توسَّع من 20 الأصلية)، كل واحد بوصف، مسار وظيفي، **حقل "مستقبل التخصص"** (أُضيف لاحقاً)، وجامعات توفره. التخصصات الرئيسية (طب، هندسة/حاسب، إدارة أعمال، قانون) مُثرَاة بفروع فرعية. نافذة تفصيلية تُفتح بالنقر.

### 3.8 المجتمع
- **لوحة الصدارة**: اختيارية، أعلى 10 بـXP.
- **غرفة المذاكرة الحيّة**: عدّاد "يذاكرون الآن" (نبضة حضور كل 25 ثانية، يُحتسَب نشطاً إن رُئي خلال آخر 90 ثانية).
- **حائط الأسئلة**: نشر سؤال/جواب عام بسيط.
- **قوالب الخطط المشتركة**: الطالب يقدر ينشر إعداد خطته (عنوان+وصف)، آخرون يتصفحون، يستخدمون (نقرة واحدة تطبّق الإعداد فوق إعدادهم)، ويقيّمون (👍/👎، مع طلب تعليق اختياري عند 👎).

### 3.9 المساعد الذكي (Gemini) — أُعيد بناؤه بالكامل أمنياً هذه المحادثة
**البنية الحالية (بعد إعادة البناء الأمنية الكاملة، انظر §6 للتفاصيل)**:
- المتصفح **لا يرسل أبداً** نموذج الذكاء الاصطناعي ولا تعليمات النظام — فقط `mode` من قائمة صارمة محدودة (`chat`, `board`, `pad`, `exam`, `title`) + البيانات الفعلية اللازمة.
- **كل تعليمات النظام والموديل مضبوطة داخل `gemini-proxy.js` على الخادم فقط** — غير قابلة للتجاوز عبر استدعاء الدالة مباشرة (تحقق فعلي أُجري: محاولة حقن `role:"system"` أو تمرير حقول `to`/`email` مباشرة، كلها تُتجاهل تماماً).
- **حد أقصى للطلبات**: 15 طلباً/5 دقائق لكل عنوان IP (جدول `gemini_rate_limit`)، يفشل بأمان (يسمح) إن تعذّر التحقق نفسه.
- **الموديل الحالي**: `gemini-3.6-flash` (بعد توقف `gemini-2.5-flash` المبكر عن الخدمة، انظر §6.9).
- **3 أدوار للدردشة الرئيسية**: حل أسئلة القدرات، الإجابة عن أسئلة الموقع، **دليل بصري** — ينقل الطالب فعلياً لأي قسم يسأل عنه عبر وسم `[[NAVIGATE:key]]` يُترجَم لتنقّل حقيقي في الواجهة.
- **قاعدة رفض نطاق صريحة** (أُضيفت بعد اكتشاف أن الذكاء الاصطناعي كان يكتب شعراً/محتوى عاماً عند الطلب): يرفض أي طلب خارج نطاق القدرات/الموقع بلطف.
- **السبورة الذكية**: يشرح خطوة بخطوة بصيغة JSON صارمة (`title`, `steps[]`, `answer`) تُعرَض كأنيميشن كتابة على سبورة تفاعلية.
- **الدفتر (Pad)**: الطالب يكتب/يرسم مسألة، الذكاء الاصطناعي يحلّلها (نص و/أو صورة) ويحلّها.
- **محادثاتي**: سجل محادثات دائم (30 كحد أقصى، 60 رسالة/محادثة)، بعناوين تُولَّد تلقائياً بالذكاء الاصطناعي.
- **الفشل الآمن**: أي عطل في Gemini يتحوّل تلقائياً لبوت أسئلة شائعة محلي قائم على الكلمات المفتاحية، بدل ترك الطالب بلا إجابة.

### 3.10 نظام البريد الإلكتروني — بُني بالكامل هذه المحادثة
- **`send-email.js`**: وسيط آمن، يتحقق من هوية الطالب فعلياً عبر توكن جلسة Supabase حقيقي (لا يثق بأي بريد يرسله المتصفح مباشرة) — نفس درس الأمان من `gemini-proxy.js` بالضبط.
- **بريد نتيجة الاختبار**: يُرسَل تلقائياً عند إنهاء اختبار محاكي **كامل فقط** (لا لفظي/كمي منفرد)، وفقط لمن ربط بريداً حقيقياً فعلاً.
- **الرسائل التذكيرية**: نظامان معاً — **جماعي** (لكل من وافق دفعة واحدة) و**ذكي فردي** (لمن غاب عدد أيام محدَّد، يُقاس من عمود `updated_at` الحقيقي في `user_data`).
- **لوحة إدارة داخل خُطى نفسه** (📧 إدارة رسائل البريد، ضمن لوحة المشرفين): كتابة/جدولة الرسائل، **إرسال تجريبي** لعناوين يحددها المطوّر (لا يمس أي طالب حقيقي، موسوم بوضوح)، **إرسال فعلي** بتأكيد صريح، **سجل إرسال كامل** (لمن/متى/نجح أم فشل).
- **خدمة الإرسال**: Brevo — مُرسِل مُتحقَّق منه: `soosrakan1430@gmail.com` (اسم العرض "خُطى"). **ملاحظة Brevo نفسها**: النطاق المجاني (Gmail) "غير موصى به" لأفضل معدل وصول — نطاق مخصَّص مستقبلاً سيحسّن هذا.
- **شروط الاستخدام وسياسة الخصوصية**: مستندان كاملان (عربي/إنجليزي)، صادقان تماماً عن الواقع (الضيف لا تُجمع عنه أي بيانات، تُذكر كل خدمة خارجية بالاسم).

### 3.11 التحفيز (Gamification)
- XP لكل مهمة مكتملة (idempotent — خريطة "already awarded" تمنع الاحتساب المزدوج).
- 6 مستويات (مستكشف ← ... ← "خبير قدرات 🏆").
- سلسلة أيام متتالية، مرتبطة **حصرياً بإكمال جلسة فعلية**، لا مجرد فتح التطبيق.
- أوسمة عادية + **أوسمة سرّية تُكتشف بالصدفة**.
- **يوم راحة أسبوعي اختياري**: لا يُحتسَب فائتاً، **لا يكسر السلسلة**، وتتوسّع كمية الأيام الأخرى تلقائياً تعويضاً.
- **احتفال بصري حقيقي عند فتح وسام جديد**: Confetti + صوت رضا مُولَّد برمجياً (Web Audio API، بلا ملف صوتي خارجي).

### 3.12 بنك الأخطاء الشخصية + اكتشاف الوهم (نظامان جديدان مترابطان)
- **سجل الأحداث السلوكية** (`khuta_behavior_log`, محلي بالكامل): يسجّل تفويت يوم، تعديل الخطة، إيقاف المؤقّت، تجاهل تنبيه الخمول، تأجيل يوم عمداً.
- **اكتشاف الوهم**: أثناء جلسة نشطة، عند خمول حقيقي (لا حركة فأرة/لمس/كتابة) لـ8 دقائق، يظهر سؤال ودّي "لسه معانا؟" — إن لم يُجَب خلال 45 ثانية، **يوقف العدّاد تلقائياً** فعلياً (لا مجرد تسجيل) لتقليل الوقت الوهمي المُحتسَب.
- **بنك الأخطاء الشخصية**: بطاقة في الملف الشخصي تكتشف أنماطاً بقواعد صريحة (لا ذكاء اصطناعي): النمط الأكثر تكراراً، تكتّل بيوم أسبوعي محدد، وتيرة تعديل الخطة، انخفاض الالتزام مع الوقت — كل رؤية مقرونة باقتراح بنّاء. حد أدنى مقصود (6 أحداث + 10 أيام) قبل أي استنتاج، تفادياً لأحكام متسرّعة.
- **تنبيه التأجيل الحقيقي**: زر "تأجيل اليوم" يحسب فعلياً (لا رقماً وهمياً) كم دقيقة إضافية سيحتاجها كل يوم متبقٍ، ويعرضه **قبل** التأكيد.

### 3.13 إمكانية الوصول (Accessibility) وتحسينات الأداء
- 34 عنصر `div`/`span` قابل للنقر أصلحت لتصبح `role="button" tabindex="0"` مع دعم لوحة مفاتيح كامل.
- `aria-live="polite"` على التوست (آلية التغذية الراجعة الأساسية في كل الموقع تقريباً) ونتيجة الاختبار — يعلن معظم الأحداث الحيّة تلقائياً لقارئ الشاشة.
- تكبير حجم الخط (صغير/متوسط/كبير) عبر `zoom` على مستوى الصفحة.
- استخراج كل CSS من `index.html` لملف `styles.css` خارجي منفصل (خفّض حجم الصفحة الأولى من ~297 إلى ~151 كيلوبايت).
- **اهتزاز/صوت/Confetti** (Micro Interactions): اهتزاز خفيف عند فشل الدخول، صوت رضا عند كل XP، Confetti عند فتح وسام — كلها تتحقق من الدعم أولاً (لا تكسر شيئاً على iOS Safari الذي لا يدعم الاهتزاز إطلاقاً)، وتحترم `prefers-reduced-motion`.

### 3.14 أدوات المطوّر واللوحة الإدارية
- **لوحة مشرفين** محمية بجدول `app_admins` (لا معرّف ثابت بالكود) — تتحقق من الصلاحية على الخادم دائماً، لا بإخفاء زر في الواجهة فقط.
- **تقارير المطوّر عبر Formspree**: مصدر مخصص جديد من طالب، أو "إرسال ملاحظة" عامة — يصل بريد المطوّر مباشرة. **منفصل تماماً** عن نظام Brevo (اتجاه مختلف: طالب→مطوّر، لا مطوّر→طالب).
- **زيارات الموقع اليومي**: لوحة إحصاء بسيطة.
- **مركز الإشعارات**: جرس + عداد غير مقروء + سجلّ دائم (30 كحد أقصى)، مربوط بأحداث حقيقية (انقطاع جلسة، وسام جديد، إنجاز 7 أيام، تذكيرات اختبار).


---

## 4. سجل الحوادث والقرارات التاريخي (الأهم لفهم "لماذا" الكود مكتوب كما هو)

هذا القسم يوثّق كل خلل حقيقي ذي شأن اكتُشف وأُصلح، مرتَّباً زمنياً تقريباً. **القيمة هنا ليست في "ماذا حدث" بل في السبب الجذري** — لأن نفس فئة الخطأ قد تتكرر إن لم يُفهَم السبب.

### 4.1 من جلسات مبكرة (قبل هذه المحادثة)

1. **منبّه المؤقّت الصاخب** → استُبدل بنغمة لطيفة (Web Audio، موجات جيبية).
2. **أوزان جامعة الملك سعود للعلوم الصحية خاطئة** (20/30/50) → صُححت للنسبة المؤكدة رسمياً (30/30/40).
3. **خلل "النقر المزدوج" في صناديق اختيار المعالج**: النقر على تسمية (label) ملفوفة حول صندوق اختيار كان يُطلق معالج الحدث مرتين (مرة للنقر المباشر، ومرة لإعادة توجيه المتصفح التلقائية من التسمية للصندوق) — يُلغي الاختيار بصمت. أُصلح بالتحوّل لـ`onchange` على الصندوق نفسه.
4. **خلل "إعادة توسيط" النوافذ المنبثقة**: `align-items:center` على الغطاء كامل الشاشة كان يجعل النافذة بأكملها "تقفز" بصرياً كلما تغيّر ارتفاع محتواها (كإظهار حقول مصدر مخصص) — بدا وكأن المحتوى "يختفي للأعلى". هذا كان أيضاً السبب الجذري لخلل ظهور القائمة على الهاتف. أُصلح بالتحوّل لتخطيط مثبَّت من الأعلى.
5. **الشريط السفلي للهاتف لم يشمل التبويبات الأحدث** (المجتمع، التخصصات) → أُصلح بجعله قابلاً للتمرير أفقياً + إضافة كل التبويبات.
6. **فشل إرسال Formspree**: السبب الجذري كان عدم "تفعيل" النموذج بعد (يتطلب تأكيد أول رسالة عبر البريد) + احتمال مشاكل CORS عند الاختبار محلياً بلا خادم. أُصلح بآلية احتياطية (iframe مخفي) + تعليمات تفعيل واضحة.
7. **"email address is invalid" من Supabase** — السبب: Supabase يتحقق من وجود سجلات DNS/MX حقيقية للنطاق، لا الصياغة فقط. نطاق وهمي مثل `khuta.local` يُرفض فوراً. أُصلح بالتحوّل لـ`gmail.com` (نطاق مضمون الصحة) كنطاق وهمي.
8. **سكربت SQL غير قابل لإعادة التشغيل** ("policy already exists" في المرة الثانية) → أُصلح بإضافة `drop policy if exists` قبل كل `create policy`، و`on conflict do nothing` لكل إدراج بيانات أولية — كل سكربتات SQL في هذا المشروع مبنية على مبدأ **idempotent** (آمنة التكرار) منذ ذلك الحين.
9. **Google Sign-In: "disallowed_useragent"** — تشخيص: Google يحجب OAuth من متصفح مضمَّن داخل تطبيق آخر (لا متصفح حقيقي).
10. **Google Sign-In: الصفحة لا تُفتح بعد إعادة التوجيه** — تشخيص: مشكلة إعداد في Google Cloud Console، لا خلل كود — "Authorized redirect URI" يجب أن يشير لرابط استدعاء Supabase نفسه، لا نطاق الموقع.
11. **خلل ترتيب الوسام/XP** (اكتُشف عبر اختبار آلي): `setAllTodayTasksStatus()` كانت تحدّث كائناً في الذاكرة وتمنح XP/تتحقق من الأوسمة **قبل** حفظ الحالة الجديدة في `localStorage` — فتحقق الأوسمة كان يقرأ دائماً حالة قديمة بخطوة واحدة، يمنع فتح الأوسمة بصمت. أُصلح بالحفظ أولاً ثم حلقة التحقق.
12. **محرّك الاختبار المحاكي**: بُني بالكامل (بنية بيانات + توزيع عشوائي متكيّف + واجهة كاملة)، لكن تُرك `FEATURE_EXAM_SIMULATOR = false` عمداً لأن الأسئلة كانت أمثلة توضيحية فقط — **فُعِّل لاحقاً بهذه المحادثة**.
13. **حادثتا تعطّل كامل للموقع بسبب القائمة الجانبية**: محاولة إخفاء القائمة افتراضياً وإظهارها بالتمرير سببت انهياراً بصرياً شاملاً مرتين. السبب الجذري النهائي (اكتُشف عبر أداة متصفح فعلية لا تخميناً): (أ) انتقال CSS على `grid-template-columns` بين `px` و`fr` عالق بلا نهاية في Chrome، (ب) بمجرد أن أصبحت القائمة `position:fixed`، كان "التموضع التلقائي" في CSS Grid يضع العمود الرئيسي في عمود خاطئ (عرض 0 فعلياً). أُصلح بإزالة الانتقال السلس نهائياً + تحديد `grid-column` صراحة بدل الاعتماد على التموضع التلقائي. **الدرس**: أي تعديل مستقبلي على `.app-container`/الشبكة يجب اختباره حياً، لا نصياً فقط.
14. **شاشة الدخول لا تختفي لمستخدمين عائدين**: كانت تُخفى فقط في فرع "لا توجد جلسة" الصريح، لا في فرع "توجد جلسة" (ظاهرة افتراضياً بـCSS). أُصلح بإخفاء صريح في الفرعين. **خلل مرتبط**: تسجيل الخروج كان يمسح الجلسة لكن ليس الاسم المحلي، فيظن الإقلاع التالي خطأً وجود "ضيف شرعي".


### 4.2 هذه المحادثة — بالتفصيل الكامل (الأحدث والأهم)

#### أ) جولة تدقيق شاملة أولى (أخطاء واجهة متفرقة)
- خلل CSS في زر الدردشة العائم (تعريف دائري قديم يتعارض مع تصميم حبّة الدواء الجديد) — الحل: حذف القاعدة القديمة نهائياً.
- **صلابة اتصال Gemini**: كان يُعطَّل نهائياً بعد أول فشل — تحوَّل لنظام "فترة تهدئة" (30 ثانية) يعيد المحاولة تلقائياً بدل التعطيل الدائم.
- مطابقة الكلمات المفتاحية العربية: أُضيف `normalizeArabic()` لمطابقة الأشكال الصرفية المختلفة (موزونتي → موزونة).
- أزرار الاختيار في محاكي الاختبار كانت بلا معالج نقر إطلاقاً (`toggleSelection` مفقودة).

#### ب) إعادة بناء واجهة الاختبار المحاكي بأسلوب "قياس" الحقيقي
إعادة بناء HTML/CSS كاملة: شريط جانبي (فعلياً على اليسار دائماً عبر حيلة `direction:ltr` على الحاوية + `direction:rtl` على الأبناء) + منطقة أسئلة رئيسية. محرّك JS جديد بالكامل (`buildSectionedExamState`, `splitIntoSections`) لدعم 5 أقسام بمؤقّت مستقل لكل قسم وتقدّم للأمام فقط.

#### ج) صقل وضع التركيز (حلقة تنفّس، انجراف نجوم، انعكاس زجاجي يتتبّع الفأرة)

#### د) إعادة بناء نظام استرجاع كلمة المرور بالكامل
اكتُشفت مشكلة خطيرة: `linkRecoveryEmail()` كانت تستبدل البريد الوهمي ببريد حقيقي عبر `updateUser({email})`، **لكن** دالة الدخول كانت **دائماً** تعيد بناء البريد الوهمي القديم من اسم المستخدم — يعني طالب ربط بريده الحقيقي كان **سيُقفَل خارج حسابه نهائياً**. الحل: جدول `username_lookup` جديد (بمشغّلات قاعدة بيانات تُزامن تلقائياً) + دالة `resolveUsernameEmail()` تستعلم الجدول أولاً قبل إعادة البناء الافتراضي — مع توافق رجعي كامل للحسابات القديمة.

#### هـ) إمكانية الوصول، SEO، تبسيط اللوحة، استخراج CSS الخارجي (تفاصيل في §3.13)

#### و) بناء نظام محادثات الذكاء الاصطناعي الدائمة (قبل إعادة البناء الأمنية)

#### ز) **الثغرة الأمنية الكبرى في `gemini-proxy.js` — اكتشاف وإصلاح كامل**
مراجعة تقنية خارجية كشفت أن الدالة الوسيطة كانت تقرأ `model` و`system_instruction` من جسم الطلب القادم من المتصفح وتمرّرهما كما هما لـGemini مباشرة. **تحقّق فعلي**: طلب مباشر للدالة (بلا فتح الموقع إطلاقاً) بتعليمات نظام مخترَعة، برد بكلمة `PROXY_OPEN` — **نجح بالكامل**. يعني أي شخص يقدر يستهلك حصة Gemini الخاصة بالموقع لأي غرض، متجاوزاً كل قيود الهوية والنطاق.

**الإصلاح الكامل** (ما زال معمارية النظام الحالية، انظر §3.9): كل تعليمات النظام والموديل صلبة داخل الدالة فقط، `mode` من قائمة صارمة، حد أقصى للطلبات لكل IP عبر جدول Supabase جديد (`gemini_rate_limit`)، وقاعدة رفض نطاق صريحة جديدة (كان الذكاء الاصطناعي يكتب شعراً عند الطلب — إصلاح مباشر لهذا أيضاً).

اختُبر الإصلاح فعلياً: محاولة حقن `role:"system"`، محاولة تمرير `email`/`to` مباشرة لتوجيه بريد لعنوان آخر — كلاهما أُثبت أنه يُتجاهَل تماماً.

#### ح) مشكلة نشر متكررة: "الملفات لا تتطابق" (نمط تكرر عدة مرات)
اكتُشف مراراً أن الموقع الحي كان يعمل بنسخة **قديمة** من ملف واحد بينما بقية الملفات كانت محدَّثة — إما بسبب نسيان رفع ملف، أو (أخطر) بسبب **تخزين متصفح مؤقت** (Cache-Control) كان يسمح بعرض نسخة قديمة لمدة ساعة كاملة. **الإصلاح**: تحويل `index.html`/`app.js`/`styles.css` إلى `no-cache` (يتحقق المتصفح من الخادم دائماً قبل الاستخدام). **الدرس المتكرر**: عند الشك في "لماذا لا يعمل التحديث"، **الفحص المباشر لملف الموقع الحي هو الطريقة الوحيدة الموثوقة** — ثبت هذا صحيحاً عدة مرات لاحقاً أيضاً (انظر ط، ي).

#### ط) عطل كامل لدالة `gemini-proxy.js` بعد النشر — "Cannot find module '@supabase/supabase-js'"
بعد إضافة اعتمادية `@supabase/supabase-js` لحساب حد الطلبات، الدالة فشلت تماماً بخطأ تحزيم رغم إدراج الحزمة في `package.json`. **الإصلاح الجذري**: إزالة الاعتمادية بالكامل، إعادة كتابة منطق حد الطلبات بـ`fetch` خالصة ضد واجهة Supabase REST مباشرة (`{SUPABASE_URL}/rest/v1/...`). **هذا الدرس طُبِّق لاحقاً على `send-email.js` من البداية** — صفر اعتماديات خارجية في كلا الدالتين الآن، تحصيناً ضد نفس فئة الخلل.

#### ي) توقّف الذكاء الاصطناعي الأول — "You exceeded your current quota" (429)
استدعاء مباشر للدالة على الموقع الحي أظهر خطأ حصة حقيقي من Google. السبب: `gemini-flash-latest` كان يشير لموديل **تجريبي** بحصة ضيقة جداً (وأظهرت الردود حقل `thoughtSignature` — وضع تفكير ممتد يستهلك حصة أكبر). **الإصلاح**: التحوّل لـ`gemini-2.5-flash` (الاسم الثابت الموصى به وقتها رسمياً للإنتاج).

#### ك) نقاش استراتيجي شامل + بناء 4 ميزات سلوكية جديدة
مناقشة 6 أفكار منتج (اكتشاف الوهم، مراجعة ذكية ليلية، "محرك قرارات دراسية"، بنك أخطاء شخصية، معاينة بيانية للساعات، تنبيه تأجيل حقيقي) — بُنيت 4 منها فعلياً (تفاصيل كاملة في §3.6، §3.12)، وأُجِّلت "المراجعة الليلية" حتى تُفعَّل الفوترة (تستهلك حصة Gemini لكل مستخدم مسجَّل كل ليلة).

#### ل) اختبار شامل مكثّف للهاتف (كل زر وقائمة)
محاكاة متصفح هاتف حقيقية (iframe بأبعاد 390×844، نافذة عرض مستقلة فعلياً — الطريقة الوحيدة التي عملت، بعد تأكيد أن أداة تصغير النافذة القياسية **لا تعمل فعلياً** في بيئة التطوير). فحص ~15 شاشة وحالة: كلها سليمة. **درس منهجي جديد**: نافذة `confirm()` داخل الإطار المحاكى تُجمِّد **التبويب بأكمله** لا الإطار فقط — يجب تعطيلها (`iwin.confirm = () => true`) قبل أي اختبار آلي مستقبلي يتضمن حوارات تأكيد.

#### م) صقل تصميمي ونفسي (بناءً على ملاحظات "أصدقاء")
اهتزاز/صوت/Confetti، نظام خط موحّد رسمي، `aria-live`، شاشة تحميل أنيقة، مناقشة الجانب النفسي (المستخدم لا يريد XP بل شعور إنجاز — تعديل صياغة النصوص لهذا مستقبلاً).

#### ن) بناء نظام البريد الإلكتروني الكامل (Brevo) — ثلاث مشاكل حقيقية بالترتيب
1. **بحث الخدمة**: قورنت Formspree (يعمل لاتجاه طالب←مطوّر فقط، يحتاج Auto Response مدفوع + نطاق مخصص لمحتوى ديناميكي) مقابل Resend/Brevo. اختيرت **Brevo** (300 بريد/يوم مجاناً دائماً، لا يشترط نطاقاً مخصصاً للبدء، يخدم الاستخدامين معاً).
2. **خلل مفتاح خاطئ**: أول مفتاح أُرسل كان **مفتاح SMTP** (`xsmtpsib-...`) لا **مفتاح REST API** (`xkeysib-...`) — نوعان منفصلان تماماً في Brevo. طُلب المفتاح الصحيح تحديداً لتفادي إضافة اعتمادية `nodemailer` (بعد درس ط أعلاه).
3. **حظر IP أمني من Brevo**: أول محاولة إرسال فعلي رجعت خطأ 403 — Brevo حجبت نداءات API من عنوان IP خادم Netlify غير المعروف مسبقاً (إجراء أمني افتراضي). أُصلح بتعطيل "Blocking unauthorized IP addresses" لمفاتيح API تحديداً من إعدادات أمان Brevo (تعطيل، لا تفويض عنوان واحد، لأن عناوين Netlify تتغيّر).
4. **⚠️ الخلل الأخطر: بريد مُرسِل غير مُتحقَّق منه** — أول محاولات إرسال فعلي بعد حل ما سبق أظهرت "نجاح" في لوحتنا الداخلية، **لكن لم يصل شيء أبداً**. السبب (اكتُشف بفحص سجل Brevo الفعلي مباشرة، لا افتراضاً): عنوان المُرسِل المكتوب بالكود (`no-reply@khutaa.netlify.app`) لم يكن مُتحقَّقاً منه إطلاقاً في حساب Brevo — Brevo تقبل الطلب (حالة "Sent") ثم ترفضه داخلياً بعد لحظات (حالة "Error"). **الدرس الحاسم**: نجاح استدعاء API (`HTTP 200`) **لا يعني تسليماً فعلياً** — التحقق الحقيقي الوحيد هو سجل التسليم الفعلي لدى المزوّد. أُصلح باستبدال المُرسِل بالبريد **المُتحقَّق منه فعلياً** (`soosrakan1430@gmail.com`، تأكد بصرياً من لوحة Brevo).

#### س) بناء لوحة إدارة رسائل البريد داخل خُطى (تفاصيل في §3.10)
خللان أمنيان/بنيويان حقيقيان اكتُشفا قبل النشر:
- `app_admins` يرتكز فعلياً على عمود `uid` لا `user_id` كما افتُرض أولاً — كان سيمنع المطوّر نفسه من دخول لوحته.
- إخفاء الزر في الواجهة **لا يكفي أمنياً** — أُضيف تحقق مستقل من صلاحية المشرف **على الخادم** داخل `send-email.js` نفسها، واختُبر فعلياً أن مستخدماً عادياً بجلسة صحيحة تماماً يُرفض بـ403.

#### ع) توقّف الذكاء الاصطناعي الثاني — "gemini-2.5-flash is no longer available to new users"
بعد أسبوعين من العمل الصحيح، توقّف الذكاء الاصطناعي كلياً. استدعاء مباشر للدالة الحية أظهر رسالة الخطأ الحرفية أعلاه من Google — **تقاعد الموديل قبل موعده الرسمي المعلَن** (16 أكتوبر)، مطابقاً تحذيراً سابقاً بوجود تقارير عن إيقاف مبكر لبعض النماذج. **الإصلاح**: التحوّل لـ`gemini-3.6-flash` (الإصدار المستقر الحالي، تحقّق من مصادر رسمية مؤرَّخة بيوم واحد فقط قبل التطبيق). عُدِّل مباشرة عبر محرّر GitHub على الويب (بعد التحقق أولاً أن الملف الحي يطابق ما سُلِّم سابقاً، تفادياً لأي تعارض غير مرئي)، ثم تحقّق فعلي بعد النشر (استدعاء الدالة الحية، رد `200` حقيقي وصل).

#### ف) اكتشاف طلب دمج أمني معلَّق (Pull Request غير مدموج)
أثناء التحقق من حالة النشر، اكتُشف أن تعديلات أمنية منفصلة (بُنيت عبر أداة "Claude Code" لا هذه المحادثة) — تثبيت نسخة Supabase SDK محدَّدة ضد هجمات سلسلة التوريد، رؤوس HSTS، `.gitignore` — كانت لا تزال **Pull Request مفتوحاً غير مدموج بـ`main`** رغم ظهورها كأنها مطبَّقة. رُوجعت المحتويات (نظيفة، منخفضة المخاطر، لا تتعارض مع أي شيء)، ورُوصي بدمجها، وزُومِنَت النسخة المحلية معها احتياطاً.

#### ص) إعادة بناء الجولة التعريفية + شاشة تحميل + تعديل افتراضي الموافقة
الفجوة الحقيقية المكتشفة: **لا توجد أي خطوة تشرح "مهام اليوم"** رغم كونه أهم عنصر يومي، والمساعد الذكي كان آخر خطوة (7 من 7) رغم طلب صريح لشرحه مبكراً. أُعيد ترتيب 8 خطوات كاملة (مهام اليوم ← ابدأ جلستك ← المساعد الذكي ← ...)، مع إصلاحين تقنيين للوضوح على الهاتف: عرض البطاقة أصبح متجاوباً (بدل 280px ثابت)، والهامش الآمن السفلي رُفع من 12 إلى 30px (حساب منطقة الإيماءة على الهواتف الحديثة). شاشة تحميل جديدة بشبكة أمان مستقلة (6 ثوانٍ كحد أقصى، لا تحبس الطالب أبداً). الموافقة على الرسائل التذكيرية أصبحت مفعَّلة افتراضياً للحسابات الجديدة تحديداً.

### 4.3 دروس عامة متكررة عبر كل هذا التاريخ (يستحق مطوّر جديد قراءتها بعناية)
1. **لا تفترض حالة الملف الحي — تحقَّق منه فعلياً** قبل أي تعديل، خصوصاً بعد أي عمل خارجي (كأدوات أخرى) على نفس المستودع.
2. **نجاح استدعاء API لا يعني نجاح التسليم الفعلي** — الأمثلة: Brevo (سجّل "Sent" لرسائل لم تصل)، ونجاح "الحفظ" بأداة اتصال معيّنة (Netlify) رغم عدم تطبيق التغيير فعلياً (اكتُشف مرتين، انظر أدناه).
3. **لا اعتماديات خارجية (npm) في الوظائف الخادمية إن أمكن تجنّبها** — `fetch` المدمجة أثبتت أنها أكثر موثوقية من أي مكتبة تحتاج تحزيماً على Netlify.
4. **إخفاء عنصر واجهة ليس تحكماً أمنياً** — أي صلاحية (مشرف، هوية طالب) يجب التحقق منها على الخادم دائماً، بغض النظر عن حالة الواجهة.
5. **الاختبار الآلي النصي (فحص بنية، جافاسكربت) ضروري لكنه غير كافٍ وحده** — عدة أخطاء حرجة (تعطّل القائمة الجانبية، خلل المُرسِل غير المُتحقَّق، تعطّل تحزيم npm) لم تُكتشف إلا بفحص مباشر لموقع حي أو خدمة خارجية فعلية.
6. **أدوات الاتصال الخارجية (Netlify MCP، متصفح آلي) قد "تنجح" ظاهرياً بلا أثر فعلي** — تحقَّق دائماً بقراءة الحالة مرة أخرى بعد أي عملية كتابة، لا الاكتفاء برسالة النجاح.


---

## 5. قاعدة البيانات (Supabase) — كل الجداول

**رابط المشروع**: `https://squhkiwjwwyrgufkaujf.supabase.co` | **معرّف المشروع**: `squhkiwjwwyrgufkaujf`

| الجدول | الغرض | ملاحظات RLS |
|---|---|---|
| `universities` | بيانات أوزان الجامعات/STEP، قابلة للتعديل عبر Table Editor | قراءة عامة فقط |
| `user_data` | نسخة مزامَنة من كل بيانات `khuta_*` المحلية لكل حساب. **يحوي الآن أيضاً `marketing_consent`** (boolean) | المالك فقط (`auth.uid() = id`) |
| `username_lookup` | يربط اسم المستخدم ببريده **الحالي** (يُحدَّث تلقائياً عبر Trigger) — أساس نظام استرجاع كلمة المرور | خادمي فقط |
| `leaderboard` | لوحة صدارة XP اختيارية | قراءة عامة؛ كتابة المالك فقط |
| `study_presence` | نبضة "من يذاكر الآن" | قراءة عامة؛ كتابة المالك فقط — **ملاحظة**: غير مستخدَم فعلياً (المجتمع يستخدم قناة Realtime Presence بدلاً منه)، جدول ميت غير ضار |
| `forum_posts` | حائط الأسئلة العام | قراءة عامة؛ أي مسجَّل (حتى مجهول) يقدر يضيف |
| `plan_templates` | قوالب خطط مشتركة | قراءة عامة؛ صاحب القالب فقط يعدّل/يحذف |
| `template_ratings` | تقييمات القوالب | قراءة عامة؛ المقيِّم فقط يضيف/يعدّل |
| `app_admins` | من هو مشرف — عمود المعرّف اسمه **`uid`** (لا `user_id`) | لا سياسات عامة، خادمي فقط |
| `custom_source_reports` | تقارير مصادر مخصصة سمّاها طلاب | خادمي فقط |
| `gemini_rate_limit` | حد 15 طلب/5 دقائق لكل IP لدالة Gemini | لا سياسات عامة، خادمي فقط |
| `email_send_log` | فترة تهدئة 5 دقائق لكل مستخدم لدالة البريد (نتائج الاختبار) | لا سياسات عامة، خادمي فقط |
| `marketing_messages` | **بنك الرسائل التذكيرية — يديره المطوّر يدوياً بالكامل من Table Editor**. الأعمدة: `subject`, `body_html`, `occasion`, `active`, `send_mode` (`broadcast`/`behavior`), `send_after`, `inactive_days`, `sent_at` | مشرفون فقط (عبر `app_admins`) |
| `email_campaign_log` | سجل كل رسالة تذكيرية أُرسلت فعلياً (لمن/متى/نجحت أم فشلت/تجريبية أم لا) | مشرفون فقط للقراءة |

**سكربتات SQL المسلَّمة** (كلها idempotent — آمنة التكرار):
- `SUPABASE_SETUP.sql` (الأصلي، الجداول الأساسية)
- `SUPABASE_MIGRATION_password_recovery.sql` (`username_lookup` + المشغّلات)
- `SUPABASE_MIGRATION_gemini_rate_limit.sql`
- `SUPABASE_MIGRATION_email_system.sql` (يشمل الجداول الثلاثة: `email_send_log`, `marketing_messages`, `email_campaign_log`, + عمود `marketing_consent`)

---

## 6. متغيّرات البيئة (Netlify → Site configuration → Environment variables)

| المتغيّر | الاستخدام | الحالة |
|---|---|---|
| `GEMINI_API_KEY` | `gemini-proxy.js` — من aistudio.google.com/apikey | ✅ مضبوط |
| `SUPABASE_SERVICE_ROLE_KEY` | `gemini-proxy.js` (حد الطلبات) + `send-email.js` (فترة التهدئة، صلاحية المشرف) | ✅ مضبوط |
| `BREVO_API_KEY` | `send-email.js` — **يجب أن يبدأ بـ`xkeysib-` لا `xsmtpsib-`** (انظر §4.2.ن) | ✅ مضبوط (بعد إعادة نشر يدوية لتفعيله) |
| `VAPID_PRIVATE_KEY` | `send-reminders.js` (تذكيرات Push للمتصفح) | ✅ مضبوط |

**⚠️ ملاحظة مهمة جداً**: تغيير متغيّر بيئة على Netlify **لا يُطبَّق تلقائياً على الوظائف المنشورة** — يحتاج **إعادة نشر فعلية** (Deploys → Trigger deploy → Deploy site) ليُدمَج المتغيّر الجديد داخل حزمة الدالة. اكتُشف هذا مرتين في هذه المحادثة (تأخير حقيقي في التشخيص كلا المرتين).

---

## 7. جرد الملفات المُرفَقة مع هذا الدليل

كل الملفات أدناه هي **أحدث نسخة محلية متطابقة (بقدر الإمكان) مع ما هو منشور فعلياً على GitHub/Netlify** حتى تاريخ هذا الدليل. الملفات التي **لم أُعدِّلها إطلاقاً** (كملفات البيانات JSON، الأيقونات، `send-reminders.js`) **غير مرفقة** — هي موجودة بالفعل في مستودعك ولم تتغيّر.

| الملف | الوجهة في GitHub | آخر تعديل جوهري |
|---|---|---|
| `index.html` | الجذر | إعادة بناء الجولة التعريفية، شاشة التحميل |
| `app.js` | الجذر | نفس الشيء + كل الأنظمة السلوكية والبريد |
| `styles.css` | الجذر | نظام الخط، شاشة التحميل، Confetti |
| `netlify.toml` | الجذر | رؤوس HSTS/Permissions-Policy (مزامَنة من PR الأمني) |
| `.gitignore` | الجذر | جديد (مزامَن من PR الأمني) |
| `gemini-proxy.js` | `netlify/functions/` | إصلاح الموديل (`gemini-3.6-flash`) |
| `send-email.js` | `netlify/functions/` | إصلاح المُرسِل + المسارات الإدارية |
| `SUPABASE_MIGRATION_email_system.sql` | لا يُرفع لـGitHub — يُشغَّل مرة في Supabase SQL Editor فقط | — |

**غير مرفَق عمداً (لم يتغيّر مطلقاً بيدي)**: `send-reminders.js`، `content.json`، `universities.json`، `specialties.json`، `exam-questions.json`، `manifest.json`، `sw.js`، الأيقونات، `robots.txt`، `sitemap.xml`، `netlify/functions/package.json`، `SUPABASE_SETUP.sql` الأصلي، `SUPABASE_MIGRATION_password_recovery.sql`، `SUPABASE_MIGRATION_gemini_rate_limit.sql`.


---

## 8. مشاكل معروفة ونقاط تحتاج انتباهاً فورياً

1. **⚠️ طلب دمج أمني مفتوح غير مدموج** — راجع `github.com/rakan1430/khuta-website/pull/1` وادمجه إن كان لا يزال مفتوحاً (نظيف، منخفض المخاطر، تحقّقت منه بنفسي).
2. **`gemini-3.6-flash` قد يحتاج مراجعة دورية مستقبلاً** — نمط توقّف الموديلات المبكر عن الخدمة تكرر مرتين فعلياً في هذا المشروع (`gemini-flash-latest` ثم `gemini-2.5-flash`). إن توقّف الذكاء الاصطناعي مجدداً، الخطوة الأولى دائماً: استدعاء الدالة مباشرة عبر console المتصفح لقراءة رسالة الخطأ الحرفية من Google (طريقة مضمونة أثبتت نجاحها مرتين).
3. **البريد يُرسَل من نطاق Gmail مجاني عبر Brevo** — يعمل، لكن Brevo نفسها تنصح بنطاق مخصص لأفضل معدل وصول (قد ينتهي بعض الرسائل بمجلد الرسائل المزعجة).
4. **جدول `study_presence` ميت** — المجتمع يستخدم قناة Supabase Realtime Presence فعلياً بدلاً منه. غير ضار، يمكن حذفه لاحقاً أو تركه.
5. **`FEATURE_EXAM_SIMULATOR` مفعَّل الآن** — تأكد أن بنك الأسئلة (`REMOTE_EXAM_QUESTIONS_URL` أو `EXAM_QUESTIONS_LOCAL`) يحوي عدداً كافياً وواقعياً، لا الأمثلة التوضيحية الأصلية فقط.
6. **موصل GitHub عبر Connector لا يعرض أدوات قراءة/كتابة فعلية** لهذه الواجهة (لوحظ عبر بحث متكرر) — التعديلات المباشرة على GitHub في هذه المحادثة تمّت عبر أداة تحكّم متصفح، لا عبر Connector مباشر. الرفع اليدوي من المستخدم يبقى الطريقة الأساسية الموثوقة.
7. **الوظيفة المجدولة لإرسال الرسائل التذكيرية تلقائياً لم تُبنَ بعد** — اللوحة الإدارية تسمح بالإرسال اليدوي (تجريبي وفعلي) فقط حالياً؛ التلقائية الدورية (اختيار عشوائي من طلاب موافقين + رسالة نشطة، بفاصل عشوائي 4-10 أيام) بقيت خطوة مستقبلية مخطَّطة.

---

## 9. ما هو مخطَّط له ولم يُبنَ بعد

- **الوظيفة المجدولة للرسائل التذكيرية التلقائية** (أعلاه) — أقرب عمل تالٍ منطقياً، يبني مباشرة فوق نظام البريد الموجود.
- **مراجعة ذكاء اصطناعي ليلية** — فكرة مؤجَّلة صراحة حتى تُفعَّل فوترة مشروع Google Cloud (تستهلك حصة Gemini لكل مستخدم مسجَّل كل ليلة، غير عملية على الحصة المجانية).
- **مراجعة شاملة لصياغة النصوص** في كل الموقع من "وظيفة" إلى "شعور" (مثال: بدل "أكمل 3 مهام" → عبارة تعكس شعور "السيطرة على جدولك") — نتيجة نقاش نفسي/تصميمي، لم يُنفَّذ بعد.
- **فحص أداء فعلي على جهاز مقيَّد** (محاكاة معالج بطيء) — لم يُختبَر مباشرة بعد.
- **نسخة تعمل بلا إنترنت (PWA كاملة/قابلة للتثبيت)** — طُلب نقاشها لا بناؤها. أساس PWA بسيط (`manifest.json`, `sw.js`) موجود من قبل، لكن العمل بلا اتصال الحقيقي (تخزين مؤقت شامل للبيانات والأصول، مزامنة لاحقة) لم يُصمَّم بعد. **جاهز لنقاش تصميمي مستقل متى طُلب**.
- **نطاق بريد مخصص لـBrevo** (بدل Gmail المجاني) — لتحسين معدل الوصول، يحتاج امتلاك نطاق أولاً.

---

## 10. مرجع سريع — "أين أجد...؟"

| أبحث عن | الملف/الموقع |
|---|---|
| منطق بناء الجدول اليومي | `app.js` → `buildScheduleTable()` |
| بيانات المصادر (عدد أقسام/وقت لكل وحدة) | `app.js` → `CONTENT_CONFIG` |
| بيانات الجامعات | جدول Supabase `universities` (أو `UNIVERSITIES` كاحتياطي بالكود) |
| تعليمات النظام لكل أدوار الذكاء الاصطناعي | `netlify/functions/gemini-proxy.js` فقط (ليست في `app.js` إطلاقاً بعد إعادة البناء الأمنية) |
| منطق إرسال البريد وكل الفحوصات الأمنية | `netlify/functions/send-email.js` |
| خطوات الجولة التعريفية | `app.js` → `ONBOARDING_STEPS` |
| أسئلة الاختبار المحاكي | `app.js` → `EXAM_QUESTIONS_LOCAL` أو `REMOTE_EXAM_QUESTIONS_URL` |
| كتابة/جدولة رسالة تذكيرية جديدة | داخل خُطى نفسه: لوحة المشرفين → 📧 إدارة رسائل البريد (أو مباشرة جدول `marketing_messages` في Supabase) |
| تفعيل/تعطيل مشرف جديد | جدول Supabase `app_admins` (عمود `uid`) |
| كل متغيّرات `khuta_*` المحلية | ابحث عن البادئة `khuta_` في `app.js` — كل واحد موثَّق بتعليق عربي عند أول استخدام تقريباً |

---

*نهاية الدليل. لأي سؤال لم تجب عليه هذه الوثيقة، أفضل نقطة بداية هي البحث المباشر في `app.js` عن الكلمة المفتاحية المتعلقة — كل نظام موثَّق بتعليقات عربية غنية فوق كل دالة رئيسية.*
